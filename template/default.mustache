#!/usr/bin/env bash
# Theme: {{theme_name}}
#
# This file is auto-generated by generate_themes.py. Do not edit directly.
# Usage: source this script in your shell to apply the theme.

# shellcheck disable=SC2034
color00="{{base00-hex-r}}/{{base00-hex-g}}/{{base00-hex-b}}"
color01="{{base01-hex-r}}/{{base01-hex-g}}/{{base01-hex-b}}"
color02="{{base02-hex-r}}/{{base02-hex-g}}/{{base02-hex-b}}"
color03="{{base03-hex-r}}/{{base03-hex-g}}/{{base03-hex-b}}"
color04="{{base04-hex-r}}/{{base04-hex-g}}/{{base04-hex-b}}"
color05="{{base05-hex-r}}/{{base05-hex-g}}/{{base05-hex-b}}"
color06="{{base06-hex-r}}/{{base06-hex-g}}/{{base06-hex-b}}"
color07="{{base07-hex-r}}/{{base07-hex-g}}/{{base07-hex-b}}"
color08="{{base08-hex-r}}/{{base08-hex-g}}/{{base08-hex-b}}"
color09="{{base09-hex-r}}/{{base09-hex-g}}/{{base09-hex-b}}"
color10="{{base10-hex-r}}/{{base10-hex-g}}/{{base10-hex-b}}"
color11="{{base11-hex-r}}/{{base11-hex-g}}/{{base11-hex-b}}"
color12="{{base12-hex-r}}/{{base12-hex-g}}/{{base12-hex-b}}"
color13="{{base13-hex-r}}/{{base13-hex-g}}/{{base13-hex-b}}"
color14="{{base14-hex-r}}/{{base14-hex-g}}/{{base14-hex-b}}"
color15="{{base15-hex-r}}/{{base15-hex-g}}/{{base15-hex-b}}"
color208="{{base208-hex-r}}/{{base208-hex-g}}/{{base208-hex-b}}"
color_background="{{background-hex-r}}/{{background-hex-g}}/{{background-hex-b}}"
color_foreground="{{foreground-hex-r}}/{{foreground-hex-g}}/{{foreground-hex-b}}"

export WALH_THEME="{{theme}}"
export WALH_MODE="{{mode}}"
export WALH_BG="#{{background-hex}}"
export WALH_FG="#{{foreground-hex}}"
export COLORFGBG="{{colorfgbg}}"

_walh_bg_hex="{{background-hex}}"
_walh_fg_hex="{{foreground-hex}}"
_walh_surface_hex="{{base00-hex}}"
_walh_dim_hex="{{base08-hex}}"
_walh_b03_hex="{{base03-hex}}"
_walh_b15_hex="{{base15-hex}}"

if [ -n "${WALH_SHELL:-}" ] && [ -f "${WALH_SHELL}/engine.sh" ]; then
  _walh_engine="${WALH_SHELL}/engine.sh"
elif [ -n "${ZSH_VERSION:-}" ]; then
  # shellcheck disable=SC2296
  eval '_walh_engine="${${(%):-%x}:A:h:h}/engine.sh"'
else
  _walh_src="${BASH_SOURCE[0]:-$0}"
  _walh_dir=""
  _walh_i=0
  while [ -L "$_walh_src" ] && [ "$_walh_i" -lt 10 ]; do
    _walh_i=$((_walh_i + 1))
    _walh_dir="$(cd -P "$(dirname "$_walh_src")" 2>/dev/null && pwd)"
    _walh_src="$(readlink "$_walh_src" 2>/dev/null)" || break
    case "$_walh_src" in
      /*) ;;
      *) _walh_src="$_walh_dir/$_walh_src" ;;
    esac
  done
  [ -z "$_walh_dir" ] && _walh_dir="$(cd -P "$(dirname "$_walh_src")/.." 2>/dev/null && pwd)"
  _walh_engine="${_walh_dir}/engine.sh"
fi

if [ -n "${_walh_engine:-}" ] && [ -f "$_walh_engine" ]; then
  # shellcheck disable=SC1090
  . "$_walh_engine"
fi
